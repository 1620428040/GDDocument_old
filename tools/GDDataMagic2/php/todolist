DataMagic后端功能设计
以后后端需要实现两个类，分别处理不同的数据结构
一类是List，用来实现独立的、结构相同的数据结构，例如无序列表、有序列表、表格等
	还需要添加记录排列顺序的功能，从而实现拖拽排序的功能。
		可以通过update `table` set `field` = `field`+1 where `field`>10实现改变所有字段的排序号码的功能
	需要添加搜索时的参数，可以设置排序、分页等

另一类是Graph，继承于List，用来实现更加复杂的结构，例如树状图、有向图
	允许不同节点拥有不同的数据结构，比如部门节点、人员节点可以存在于同一张图中
		节点的结构{...,"parentType":"","parent":"",...}
		但是一般情况下，在一个树状图中，最多也就两种节点，比如部门和人员，而且人员只能作为部门的子节点（端点、末端、叶片），所以这里parentType未指定的话，就直接默认为就是部门，parent就是部门ID
	将数据的结构作为有向图进行处理的，允许两个数据之间添加联系
		addRelation(parent,child);如果child节点不存在，就创建一个新的
	由于有向图有时候会有循环结构，所以没法以树状图的形式输出全部数据，只能将数据传到前端处理
		
	